// ✅ Level 1: Basic Practice (Math Basics)

// Generate a random number between 1 and 100 using Math.random() and Math.floor().

// Round 4.6 to the nearest whole number using Math.round().

// Find the square root of 144 using Math.sqrt().

// Calculate 3 raised to the power 4 using Math.pow().

// Round 7.2 up using Math.ceil().

// Round 7.9 down using Math.floor().

// Get the absolute value of -25 using Math.abs().

// Round all numbers in an array [2.3, 5.8, 9.9, 6.2] to the nearest integer.

// Use Math.min() to find the smallest number among 45, 22, 87.

// Use Math.max() to find the largest number among 23, 109, 88.






// 🔁 Level 2: Logic-Based Tasks

// Simulate a dice roll (number between 1 and 6).

// Simulate a coin toss that randomly prints "Heads" or "Tails".

// Create a random 4-digit OTP generator using Math.random().

// Write a function that gives a random even number between 10 and 100.

// Generate a random number between 100 and 500.

// Create a program to check if a number is a perfect square using Math.sqrt().

// Generate a random odd number between 1 and 99.

// Create a function that generates 5 random numbers between 1 and 50 and returns them in an array.

// Simulate a game: if the random number between 1 to 10 is 7, print "You Win!", else "Try Again".

// Write a program that calculates the distance between two points (x1, y1) and (x2, y2) using the formula
// √((x2 − x1)^2 + (y2 − y1)^2)





// 🔠 Level 3: Small Challenges

// Write a program to generate a random password of 6 numbers (0–9).

// Create a program that finds the cube root of a number using Math.pow(n, 1/3).

// Simulate a random color code generator (e.g., #A1B2C3).

// Generate 10 random numbers between 1 and 100, then print the highest one.

// Create a logic that only generates even numbers between 100 and 700.

// Create a logic that rounds a floating number randomly up or down using Math.random() with Math.floor() and Math.ceil().

// Generate a random time (hours and minutes) like 14:35 using Math.random().

// Use Math.sign() to check if a number is positive, negative, or zero.

// Find the average of 5 random numbers.

// Generate a number between 0 and 10 and convert it into stars (e.g., ★★★★★ for 5)